# ==================== 核心框架 ====================
streamlit>=1.28.0
plotly>=5.17.0
pandas>=2.0.0
numpy>=1.24.0

# ==================== 深度学习（可选，默认不装）====================
# ⚠️ torch 全家桶约 500 MB+，是整体依赖里最重的一块，只被 core/model_predictor.py 使用。
#    该模块是延迟加载的（app.py 中经 _get_predictor() 按需导入，且有 try/except 保护），
#    缺失时会自动降级为模拟数据（_generate_mock_history / _predict_mock）。
#    因此默认从部署依赖里移除，避免拖慢构建、撑爆内存。
#    需要本地跑 LSTM 预测时再单独安装：
#        pip install -r requirements-lstm.txt
# torch>=2.0.0
# torchvision>=0.15.0
# torchaudio>=0.15.0

# ==================== 统计模型 ====================
statsmodels>=0.14.0

# ==================== 地图与可视化 ====================
folium>=0.14.0
streamlit-folium>=0.15.0

# ==================== 数据处理 ====================
openpyxl>=3.1.0
xlrd>=2.0.0
influxdb-client>=1.40.0
paho-mqtt>=2.0.0

# ==================== 用户认证与安全 ====================
bcrypt>=4.0.0                          # 密码哈希加密

# ==================== 云数据库 (Supabase) ====================
supabase>=2.0.0
postgrest>=0.10.0
realtime>=0.1.0

# ==================== 环境变量管理 ====================
python-dotenv>=1.0.0

# ==================== 多源数据接入（5.4 任务）====================
pymysql>=1.1.0                         # MySQL 接入
psycopg2-binary>=2.9.0                 # PostgreSQL 接入
requests>=2.31.0                       # REST API 请求

# ==================== 其他工具 ====================
pypinyin>=0.50.0                       # 省份拼音分组（用户端用）
